# Controlled experiments

This directory contains the reviewer-facing regeneration utility for the controlled-experiment outputs reported in the manuscript.

- `regenerate_results.py` rebuilds tables and figures from the committed machine-readable results under `results/controlled/` and checks the reported theorem-verification summary.

The exact orchestration code that produced the final controlled JSON outputs is not included in this package.
